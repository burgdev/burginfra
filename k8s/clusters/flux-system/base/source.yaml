apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: git-burginfra-base
  namespace: flux-system
spec:
  interval: 10m
  url: ssh://git@github.com/burgdev/burginfra.git
  ref:
    branch: flux
  secretRef:
    name: flux-system
