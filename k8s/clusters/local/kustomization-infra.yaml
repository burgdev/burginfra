apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-traefik
  namespace: flux-system
spec:
  interval: 10s
  path: "./k8s/infrastructure/traefik"  
  prune: true
  sourceRef:
    kind: GitRepository
    name: local-gitops
  postBuild:
    substitute:
      EMAIL: "tobias@burgdev.ch"
    #substituteFrom:
    #  - kind: ConfigMap
    #    name: local-cluster-vars
    #    optional: true
    #  - kind: Secret
    #    name: local-cluster-secret-vars
    #    optional: false
