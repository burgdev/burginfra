apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps
  namespace: flux-system
spec:
  #interval: 10s
  path: "../../apps/whoami/overlays/local"       # relative path from flux-system folder to your apps
  prune: true
  sourceRef:
    kind: GitRepository
    name: local-gitops
  postBuild:
    substitute:
      DOMAIN: "whoami.burginfra"
    #substituteFrom:
    #  - kind: ConfigMap
    #    name: local-cluster-vars
    #    optional: true
    #  - kind: Secret
    #    name: local-cluster-secret-vars
    #    optional: false
