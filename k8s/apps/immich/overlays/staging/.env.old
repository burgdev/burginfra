# Domain for the Ingress
DOMAIN=pix.local.crux.li
#DOMAIN=pix.burginfra
ENV=local

# Host path where Immich will store its data
# Make sure this directory exists and has the right permissions

#HOST_PATH=/home/tobias/mnt/k8s/immich/library
HOST_PATH=/mnt/kubernetes/volumes/immich/server/library
STORAGE_SIZE=5Gi

MAPTILER_KEY=Qw5uULMWH4eI8iOp0mtc

EXTERNAL_DOMAIN=https://pix.local.crux.li
# TODO: add e-mail and url
WELCOME_TEXT_HTML="<b>Welcome</b> to <i>pix.local.crux.li</i>"

# Log level (verbose, debug, log, warn, error)
IMMICH_LOG_LEVEL=log

# E-Mail
EMAIL_ENABLED=true
EMAIL_FROM="pix.crux.li <no-reply@localhost>"
EMAIL_HOST="mail.infomaniak.com"
EMAIL_HOST_PASSWORD="Cleat2\$Yelling\$Overstate"
EMAIL_HOST_USER=tb@wodore.com
EMAIL_HOST_PORT=465

