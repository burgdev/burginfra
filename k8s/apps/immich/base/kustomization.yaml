apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - postgres.yaml
  - pvcs.yaml
  - helmrelease.yaml
  # - backups.yaml

configMapGenerator:
  - name: immich-values
    files:
      - values.yaml=values.yaml
    options:
      disableNameSuffixHash: true
