apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: podinfo-image-media-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast-media
  resources:
    requests:
      storage: 10Mi
