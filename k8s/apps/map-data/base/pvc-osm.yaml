apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: map-data-osm
  namespace: ${NAMESPACE}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast-local-data-v3
  resources:
    requests:
      storage: ${MAP_DATA_OSM_STORAGE_SIZE:-10Gi}
