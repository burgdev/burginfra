apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: podinfo-data # 'username' for kopia
spec:
  trigger:
    schedule: "*/5 * * * *" # every 5 minutes
  kopia:
    # Overwrite Retention Policy
    retain:
      hourly: 24
      daily: 2
      weekly: 2
      monthly: 1
      yearly: 0