apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: podinfo-fast-data-pvc-v2 # update deployment.yaml
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast-local-data-v2 # update metadata.name
  resources:
    requests:
      storage: 50Mi
