apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fast-data-pvc-v1 # update deployment.yaml
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast-local-data-v1 # update metadata.name
  resources:
    requests:
      storage: 50Mi