apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wd-imagor
  annotations:
    cert-manager.io/cluster-issuer: ${CERT_ISSUER_PROD}
spec:
  rules:
    - host: img.${ENV_SUBDOMAIN}${WODORE_HOST}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wd-imagor
                port:
                  number: 8000
  tls:
    - hosts:
        - img.${ENV_SUBDOMAIN}${WODORE_HOST}
      secretName: wd-imagor-tls
