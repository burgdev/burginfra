apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-wodore-production
  namespace: flux-system # do not change!
spec:
  interval: 3h0s
  retryInterval: 1m # Retry quickly when dependencies update or reconciliation fails
  path: "./k8s/apps/wodore/overlays/production"
  prune: false
  dependsOn:
    - name: apps-sync-all-production
  healthChecks:
    - apiVersion: batch/v1
      kind: Job
      name: wd-backend-migrate
      namespace: production
  timeout: 10m # Overall timeout for reconciliation including health checks
  sourceRef: # k8s/clusters/local/flux-system/gotk-sync.yaml
    kind: GitRepository
    name: burgdev-burginfra
  postBuild:
    substitute:
      NAMESPACE: "production"
      ENVIRONMENT: "prod"
      ENV_SUBDOMAIN: ""
      WD_BACKEND_TAG: "edge-alpine"
    substituteFrom:
      - kind: ConfigMap # takes configmap from namespace flux-system
        name: cluster-settings
      - kind: Secret
        name: openbao-configs-heartbeats
        #- kind: Secret
        #  name: wodore-production-config-secret
