apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-volsync-staging
  namespace: flux-system # do not change!
spec:
  interval: 1h0s
  path: "./k8s/infrastructure/volsync/base"
  dependsOn:
  - name: helm-volsync-system
  - name: infra-openebs-local 
  prune: true
  sourceRef: # k8s/clusters/local/flux-system/gotk-sync.yaml
    kind: GitRepository
    name: git-burginfra-dev
  postBuild:
    substitute:
      NAME: "volsync"
      NAMESPACE: "staging"
      COMPONENT: "backup"
      ENVIRONMENT: "staging"
