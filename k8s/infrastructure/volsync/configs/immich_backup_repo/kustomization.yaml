apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: production
secretGenerator:
  - name: volsync-immich-kopia-webdav-secrets
    envs:
      - .env.secret
    options:
      disableNameSuffixHash: true
