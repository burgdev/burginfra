## not supported
#apiVersion: traefik.containo.us/v1alpha1
#kind: Middleware
#metadata:
#  name: longhorn-ui-basic-auth
#  namespace: longhorn-system  # adjust namespace
#spec:
#  basicAuth:
#    secret: longhorn-ui-auth-secret
#    # optional: removeHeader: true
#---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longhorn-ui
  annotations:
    #traefik.ingress.kubernetes.io/router.middlewares: longhorn-system-longhorn-ui-basic-auth@kubernetescrd
  #  cert-manager.io/cluster-issuer: letsencrypt-prod
  #  traefik.ingress.kubernetes.io/router.tls.certresolver: default
spec:
  rules:
  - host: ${DOMAIN}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: longhorn-frontend
            port:
              number: 80
  #tls:
  #  - hosts:
  #    - ${DOMAIN}
