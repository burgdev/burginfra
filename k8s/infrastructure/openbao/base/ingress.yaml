apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openbao-ui
  annotations:
    cert-manager.io/cluster-issuer: ${CERT_ISSUER_PROD}
spec:
  rules:
    - host: kms.${ENV_SUBDOMAIN}${BURGINFRA_HOST}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: openbao-ui
                port:
                  number: 8200
  tls:
    - secretName: openbao-ui-${ENVIRONMENT}-tls
      hosts:
        - kms.${ENV_SUBDOMAIN}${BURGINFRA_HOST}
